generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root@localhost:3306/request"
}

model request {
  id    Int     @id @default(autoincrement())
  name  String
}